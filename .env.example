# Neon PostgreSQL — salin dari Vercel > Storage > Neon > .env.local
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# Optional: password untuk /api/admin/* endpoint
ADMIN_SECRET="ganti-dengan-secret-yang-kuat"
